public enum SceneType
{
    Login,
    Main,
    World,
    Card,
    Gacha
}

public enum Rareness
{
    N,
    R,
    SR
}

public enum Type
{
    Red,
    Blue,
    Yellow
}

public enum Filter
{
    속성,
    레어도,
    체력,
    공격력
}
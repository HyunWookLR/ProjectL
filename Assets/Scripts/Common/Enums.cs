public enum SceneType
{
    Login,
    Main,
    World,
    Card,
    Gacha
}
public enum AttributeColor
{
    Red,
    Blue,
    Yellow
}

public enum Rareness
{
    N,
    R,
    SR
}

public enum Filter
{
    레어도,
    체력,
    공격력
}